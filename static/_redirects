# Cutover: docs are now served from https://8labs.id
# Paths are preserved 1:1 (/docs/*, /guides/*, /blog/*).
# Legacy pre-split URLs and old category/tag/utility URLs are
# handled by 8labs.id's own _redirects after forwarding.
/* https://8labs.id/:splat 301
